# 按需工具目录

本目录只登记任务需要时调用的工具 skills；规范类 skills 直接登记在根目录 `AGENTS.md` 的规范摘要表中。

目前尚未登记按需工具。后续条目采用以下字段，不将示例或环境中所有可用 skills 自动登记为项目工具。

| 名称 | 用途摘要 | 必要的适用条件 | 入口 |
| --- | --- | --- | --- |

用途摘要以自然语言概括能力，支持语义检索，不穷举触发词。工具较多时可按能力领域分组。选中候选后再读取对应 `SKILL.md`，确认环境与任务适用性；一次执行结束后停止应用流程，登记继续保留。
