<!-- project-manager:begin -->
## Project Manager 协作约定

- 根据当前任务目标、操作对象及仍在进行的工作，语义匹配下表中的规范职责。摘要不是穷举触发场景；开展相关工作前读取对应规范，不必遍历 skill 目录。
- 涉及多个领域时分别应用对应规范；任务明确退出某领域后停止应用其专属规则，再次进入时恢复适用。临时提问不自动取消仍在进行的任务。

### 规范 Skills

本表只登记规范类 skills，具体规则按匹配结果读取；不列出按需工具。

| 名称 | 职责摘要 | 入口 |
| --- | --- | --- |
| skill-rules | 项目 skills 的接入、存放、分类、适用范围及规范表和工具目录维护；已提供基础登记约定 | `.project-manager/skill/skill-rules/SKILL.md` |
| document-rules | 项目文档的浅层能力概览、按功能建档与已有文档纠正；目标、现状、计划分开维护 | `.project-manager/skill/document-rules/SKILL.md` |

### 按需工具检索

任务需要自动化能力时，先查阅或检索工具目录 `.project-manager/skill/tools.md`，按任务意图和用途摘要选择候选工具，再读取对应 skill 确认适用环境并执行。无需用户准确点名工具；普通任务不预读工具目录，也不遍历全部 skill 文件。没有匹配项时不假定能力已配置；仅在需要查找、接入能力或排查登记失效时进一步检查来源。工具执行结束后停止应用其流程，目录登记保留供后续使用。

### 文档与权限

- 项目文档位于 `.project-manager/document/`，存放目标、现状与阶段计划；已有 `index.md` 时从该入口按任务查阅。首次建档只做浅层概览，指定功能或完整整理才深入；相关实现或认知变化时按文档规范修订已有文档。目标表达意图，现状记录事实，计划不等于实施授权。
- 登记、读取或临时调用 skill 不扩大授权，也不自动成为长期规范。改变长期规范及适用范围须有相应授权，已有授权不重复询问。
- 管理 skill 若标明尚未实现，不将其视为已具备完整流程；遵循用户当前明确要求。路径缺失或规范冲突时说明具体情况，不假装已加载，不自行改写长期规则。遵循宿主指令优先级与工具权限。
<!-- project-manager:end -->
