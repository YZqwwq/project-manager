<!-- project-manager:begin -->
## Project Manager 协作约定

### 文本规则的加载与退出

根据当前任务的目标、操作对象和仍在进行的工作，语义匹配下表中的适用范围，读取相关文本规则。规则是轻量的项目方向补充，不是完整执行方案。

- 同一会话已读取且未变化、当前上下文仍可用的规则沿用，不逐轮重复读取；规则更新或上下文缺失时重新读取。
- 涉及多个领域时分别应用；明确退出某领域或超出规则有效期后停止应用。临时提问不自动取消仍在进行的任务。
- 卸载表示停止适用，不表示删除规则文件或从上下文物理移除内容。参考文件和 skill 不因被规则引用就自动全部加载。

| 领域 | 适用范围与有效期 | 规则文件 |
| --- | --- | --- |
| 规则编写 | 创建、修改或审阅文本规则时；长期适用 | `.project-manager/rule/README.md` |

目前没有登记业务领域文本规则。规则统一放在 `.project-manager/rule/`，不从项目技术栈自行推导用户偏好。

### 按需 Skills

需要专业规范、工作方法或自动化能力时，查阅或检索 `.project-manager/skill/index.md`，根据职责摘要和适用条件选择 skill，再读取具体入口。规范类与工具类均按需读取使用，不在本文件展开正文或维护逐项摘要，不遍历整个 skill 目录。

- 用户明确指定某个 skill 用于当前项目任务，或 Agent 决定实际使用未登记的 skill 时，先检查能力地图，并按 `.project-manager/skill/manage-skills/SKILL.md` 完成收录：全局登记实际路径，非全局完整复制到项目目录后登记并使用副本。已有条目直接复用；只讨论、评价 skill 而不用于项目任务时不收录。用户明确要求只读或不落盘时不复制登记，说明本次仅直接参考。收录不等于变成常驻规则，也不授权额外业务操作。
- 添加、调整 skills 或文本规则时，从能力地图选择 skill 管理能力；建立、维护项目文档或纠正已有理解时，选择文档管理能力。
- 已加载且未变化、当前上下文仍可用的 skill 不逐轮重读；退出其适用任务后停止应用。选择与执行遵循当前任务授权，登记不等于立即执行。
- 地图无匹配项时不假定能力已配置，只有任务需要查找、接入或排查失效时进一步检查来源。

### 文档与权限

项目文档从 `.project-manager/document/index.md`（若存在）按需查阅。首次建档只做浅层概览，指定功能或明确完整整理才深入；相关实现或认知变化时按文档能力修订已有文档。目标、现状与计划分开，计划不构成执行授权。

临时补充不自动成为长期规则。持久化或改变规则范围需符合用户当前要求和已有授权；已有授权不重复询问。路径缺失或规则冲突时说明具体情况，遵循宿主指令优先级和工具权限。
<!-- project-manager:end -->
